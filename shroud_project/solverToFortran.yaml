library: FortranBindings
cxx_header: "FortranBindings.hpp"

declarations:
  # ==========================================
  # Initialization: initImmersedEq (Generics kept)
  # ==========================================
  - decl: |
      void initImmersedEq_d_i32(          
          size_t height, 
          size_t width, 
          size_t depth,
          size_t fSize +implied(size(f)), 
          size_t nnzMaxB,
          double* p +intent(in) +rank(1),          
          double* f +intent(in) +rank(1),
          double deltaX,
          double deltaY,
          double deltaZ,
          double tolerance, 
          size_t maxBCGIterations
      )
    
  - decl: |
      void initImmersedEq_s_i32(
          size_t height, 
          size_t width, 
          size_t depth,
          size_t fSize +implied(size(f)), 
          size_t nnzMaxB,          
          float* p +intent(in) +rank(1),          
          float* f +intent(in) +rank(1),          
          double deltaX, 
          double deltaY, 
          double deltaZ,          
          double tolerance, 
          size_t maxBCGIterations
      )
    
  - decl: |
      void initImmersedEq_d_i64(          
          size_t height, 
          size_t width, 
          size_t depth,          
          size_t fSize +implied(size(f)), 
          size_t nnzMaxB,          
          double* p +intent(in) +rank(1),          
          double* f +intent(in) +rank(1),          
          double deltaX, 
          double deltaY, 
          double deltaZ,          
          double tolerance, 
          size_t maxBCGIterations
      )
    
  - decl: |
      void initImmersedEq_s_i64(
          size_t height, 
          size_t width, 
          size_t depth,          
          size_t fSize +implied(size(f)), 
          size_t nnzMaxB,          
          float* p +intent(in) +rank(1),
          float* f +intent(in) +rank(1),
          double deltaX, 
          double deltaY, 
          double deltaZ,          
          double tolerance, 
          size_t maxBCGIterations
      )
    
  # ==========================================
  # Solver: solveImmersedEq (Specific calls only)
  # ==========================================
  - decl: |
      void solveImmersedEq_d_i32(
          double* result +intent(out) +rank(1),
          size_t nnzB,          
          int32_t* rowPointersB +intent(in) +rank(1),          
          int32_t* colPointersB +intent(in) +rank(1),          
          double* valuesB +intent(in) +rank(1),
          bool multiStream
      )

  - decl: |
      void solveImmersedEq_s_i32(
          float* result +intent(out) +rank(1),
          size_t nnzB,          
          int32_t* rowPointersB +intent(in) +rank(1),          
          int32_t* colPointersB +intent(in) +rank(1),          
          float* valuesB +intent(in) +rank(1),
          bool multiStream
      )

  - decl: |
      void solveImmersedEq_d_i64(
          double* result +intent(out) +rank(1),
          size_t nnzB,          
          int64_t* rowPointersB +intent(in) +rank(1),          
          int64_t* colPointersB +intent(in) +rank(1),          
          double* valuesB +intent(in) +rank(1),
          bool multiStream
      )

  - decl: |
      void solveImmersedEq_s_i64(
          float* result +intent(out) +rank(1),
          size_t nnzB,          
          int64_t* rowPointersB +intent(in) +rank(1),          
          int64_t* colPointersB +intent(in) +rank(1),          
          float* valuesB +intent(in) +rank(1),
          bool multiStream
      )